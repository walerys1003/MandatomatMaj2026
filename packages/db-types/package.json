{
  "name": "@mandatomat/db-types",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./database": "./src/database.types.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "generate": "supabase gen types typescript --local > src/database.types.ts",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "@mandatomat/config": "workspace:*",
    "typescript": "^5.6.2"
  }
}
